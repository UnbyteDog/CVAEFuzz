# CVDBFuzz 项目依赖包
# 基于CVAE生成与DBSCAN优化的智能Web模糊测试框架

# 核心深度学习框架
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 数据处理和科学计算
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.2.0

# 聚类算法
scipy>=1.9.0

# 进度条和可视化
tqdm>=4.64.0
matplotlib>=3.5.0
seaborn>=0.11.0

# 配置文件处理
pyyaml>=6.0

# 命令行参数解析（内置，无需安装）
argparse

# JSON处理（内置，无需安装）
json

# 路径处理（内置，无需安装）
pathlib

# 类型提示（内置，无需安装）
typing

# 集合和计数器（内置，无需安装）
collections

# 正则表达式（内置，无需安装）
re

# 序列化（内置，无需安装）
pickle

# 随机数生成（内置，无需安装）
random

# 时间处理（内置，无需安装）
time
datetime

# 数学计算（内置，无需安装）
math

# 系统交互（内置，无需安装）
sys
os

# 文本处理（内置，无需安装）
string
unicodedata

# 可选：GPU加速支持
# nvidia-ml-py3>=7.352.0  # GPU监控

# 可选：分布式训练支持
# accelerate>=0.20.0
# datasets>=2.10.0

# 可选：更好的内存管理
# psutil>=5.9.0

# 可选：日志记录
# loguru>=0.6.0