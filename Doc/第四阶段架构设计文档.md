# CVDBFuzz 第四阶段架构设计文档

## 文档信息

- **项目名称**: CVDBFuzz - 基于CVAE生成与DBSCAN优化的智能Web模糊测试框架
- **阶段**: 第四阶段 - 三引擎并行模糊测试系统
- **版本**: v1.0
- **作者**: 老王 (暴躁技术流)
- **创建日期**: 2025-12-25
- **最后更新**: 2025-12-25

---

## 目录

- [1. 概述](#1-概述)
- [2. 目录结构设计](#2-目录结构设计)
- [3. 核心模块设计](#3-核心模块设计)
- [4. 技术决策与约束](#4-技术决策与约束)
- [5. 数据流设计](#5-数据流设计)
- [6. 扩展性设计](#6-扩展性设计)
- [7. 实施路线图](#7-实施路线图)

---

## 1. 概述

### 1.1 阶段目标

第四阶段旨在构建一个**三引擎并行**的智能Web模糊测试系统，支持以下核心功能：

- ✅ **多引擎支持**: 自研引擎(BasicFuzz) + Boofuzz引擎 + ffuf引擎
- ✅ **Spider集成**: 接收爬虫模块(Fuzz/Spider)生成的FuzzTarget列表
- ✅ **灵活字典加载**: 支持单文件/多文件/文件夹/通配符模式
- ✅ **双模式运行**: 普通模式(专家字典) + CVAE模式(AI生成载荷)
- ✅ **实时进度显示**: 基于tqdm的进度条，显示扫描状态
- ✅ **结果持久化**: 实时写入JSON文件，防止数据丢失
- ✅ **结果分析评估**: 扫描结束后自动生成分析报告

### 1.2 MVP范围(第一版)

**包含功能**:
- ✅ 自研基础引擎(BasicFuzz)完整实现
- ✅ SQL注入引擎 + XSS检测引擎
- ✅ Spider集成(单目标/整站)
- ✅ CVAE模式 + 普通模式
- ✅ 串行引擎调度(先SQLi，后XSS)
- ✅ 单目标多参数并发测试
- ✅ 实时进度显示
- ✅ 结果实时写入

**后续版本**:
- 🔄 Boofuzz引擎集成
- 🔄 ffuf引擎集成
- 🔄 多引擎并行调度
- 🔄 多目标并发测试
- 🔄 HTML报告生成
- 🔄 命令注入引擎(CMDi)

---

## 2. 目录结构设计

### 2.1 完整目录树

```
CVDBFuzz/
├── fuzzmain.py                          # [顶层入口] 统一调度入口
├── config/                              # [配置目录] 全局配置文件
│   ├── settings.yaml                    # 全局配置
│   └── engines.yaml                     # 引擎配置
│
└── Fuzz/                                # [核心业务] Fuzz核心包
    ├── spider.py                        # [已有] 爬虫引擎
    │
    ├── BaseFuzz/                        # [核心] 自研基础引擎
    │   ├── __init__.py
    │   │
    │   ├── engine.py                    # [调度器] 引擎加载与任务分发
    │   ├── requester.py                 # [通信中枢] HTTP请求与Session管理
    │   ├── baseline.py                  # [基准组件] 响应指纹与异常检测
    │   │
    │   ├── engines/                     # [检测引擎] 漏洞检测插件
    │   │   ├── __init__.py
    │   │   ├── base_engine.py           # 抽象基类
    │   │   ├── sqli_engine.py           # SQL注入引擎(报错/布尔/时间)
    │   │   ├── xss_engine.py            # XSS检测引擎(反射型/上下文分析)
    │   │   └── cmdi_engine.py           # 命令注入引擎(第二版)
    │   │
    │   ├── loaders/                     # [载荷层] 载荷加载与转换
    │   │   ├── payload_manager.py       # 载荷管理器
    │   │   └── transformer.py           # 载荷转换器(第二版)
    │   │
    │   ├── analysis/                    # [分析层] 结果评估与报告
    │   │   ├── analyzer.py              # 分析器(去重/评级/统计)
    │   │   ├── reporter.py              # 报告生成器(JSON/HTML)
    │   │   └── validators.py            # 结果验证器(第二版)
    │   │
    │   └── utils/                       # [工具层] 公共工具
    │       ├── logger.py                # 日志系统
    │       ├── progress.py              # 进度显示封装
    │       ├── config.py                # 配置管理
    │       └── constants.py             # 常量定义
    │
    ├── BooFuzz/                         # [适配层] Boofuzz引擎(第二版)
    │   ├── __init__.py
    │   ├── boofuzzmain.py               # Boofuzz启动入口
    │   ├── protocol/                    # 协议定义
    │   │   └── http_proto.py
    │   └── monitors/                    # 监控器
    │       └── crash_monitor.py
    │
    ├── ffuf/                            # [适配层] ffuf引擎(第二版)
    │   ├── __init__.py
    │   ├── ffufmain.py                  # ffuf启动入口
    │   ├── wrapper.py                   # CLI封装器
    │   ├── parser.py                    # JSON结果解析器
    │   └── progress.py                  # 实时进度解析
    │
    └── Common/                          # [公共模块] 跨引擎公共组件
        ├── __init__.py
        ├── interface.py                 # 抽象基类接口
        ├── payload_loader.py            # 统一载荷加载器
        └── utils.py                     # 公共工具函数
```

### 2.2 目录职责说明

#### 2.2.1 fuzzmain.py (顶层入口)

**职责**:
- 解析命令行参数
- 初始化日志系统
- 根据参数选择引擎(Basic/BooFuzz/ffuf)
- 调用Spider生成FuzzTarget
- 启动扫描任务
- 生成最终报告

**关键参数**:
```bash
--scan                    # 启动扫描模式
--url <url>              # 目标URL
--engine <engine>        # 引擎选择: basic/boofuzz/ffuf
--mode <mode>            # 模式选择: common/cvae
--payloads <source>      # 载荷源(文件/文件夹/通配符)
--cookie <cookie>        # 全局Cookie
--threads <num>          # 并发线程数
--timeout <sec>          # 请求超时时间
--depth <num>            # 爬虫深度(整站模式)
--output <file>          # 结果输出文件
```

#### 2.2.2 Fuzz/BaseFuzz/ (自研引擎)

**核心设计理念**:
- **分层架构**: 调度层 → 通信层 → 检测层 → 分析层
- **插件化**: engines/目录下的检测引擎可独立开发
- **职责分离**: 每个模块职责单一，高内聚低耦合

**模块说明**:

| 模块 | 文件 | 职责 | 关键类/函数 |
|------|------|------|------------|
| 调度器 | engine.py | 加载引擎、分发任务、收集结果 | Engine.load(), Engine.run() |
| 通信中枢 | requester.py | HTTP请求、Session管理、URL净化 | Requester.send(), Requester.clean_url() |
| 基准组件 | baseline.py | 建立响应指纹、对比异常 | Baseline.establish(), Baseline.compare() |
| SQL引擎 | sqli_engine.py | 报错/布尔/时间盲注检测 | SQLiEngine.detect() |
| XSS引擎 | xss_engine.py | 反射型XSS、上下文分析 | XSSEngine.detect_reflected() |
| 载荷管理 | payload_manager.py | 加载载荷、按类型分组 | PayloadManager.load() |
| 结果分析 | analyzer.py | 去重、评级、统计 | Analyzer.analyze() |

#### 2.2.3 Fuzz/BooFuzz/ (Boofuzz适配层)

**职责**:
- 封装Boofuzz的Python API
- 定义HTTP协议结构
- 实现监控器(监控Crash/异常)
- 转换FuzzTarget为Boofuzz任务

**关键功能**:
```python
BooFuzzEngine.run(targets, payloads):
    # 定义HTTP协议
    # 定义监控器
    # 启动Boofuzz会话
    # 解析结果
```

#### 2.2.4 Fuzz/ffuf/ (ffuf适配层)

**职责**:
- 封装ffuf CLI工具
- 通过subprocess调用
- 实时解析stdout进度
- 解析JSON输出结果

**关键功能**:
```python
FfufWrapper.run_scan(url, wordlist, output_file):
    # 构建ffuf命令
    # 启动subprocess
    # 解析JSON输出
    # 返回统一格式结果
```

#### 2.2.5 Fuzz/Common/ (公共模块)

**职责**:
- 定义统一的抽象基类接口
- 实现跨引擎的公共工具
- 统一载荷加载逻辑

**核心接口**:
```python
class BaseFuzzEngine(ABC):
    @abstractmethod
    def load_payloads(self, source, mode):
        pass

    @abstractmethod
    def scan(self, targets):
        pass

    @abstractmethod
    def analyze(self):
        pass
```

---

## 3. 核心模块设计

### 3.1 engine.py - 引擎调度器

**设计目标**:
- 负责加载漏洞检测引擎
- 串行调度引擎(先SQLi，后XSS)
- 协调Requester、Baseline、PayloadManager

**核心类**:
```python
class Engine:
    """基础模糊测试引擎调度器"""

    def __init__(self, config: Dict)
    def load(self, engine_names: List[str])           # 加载引擎
    def run(self, targets: List[FuzzTarget], mode: str) # 运行扫描
    def dispatch(self, target, payloads, engine)      # 分发任务
    def collect(self) -> List[Dict]                   # 收集结果
```

**调度流程**:
```
1. 加载引擎: load(['sqli', 'xss'])
2. 加载载荷: PayloadManager.load(mode='cvae')
3. 遍历目标: for target in targets:
    3.1 建立基准: baseline.establish(target)
    3.2 串行执行引擎:
        - sqli_engine.detect(target, payloads)
        - xss_engine.detect(target, payloads)
    3.3 实时写入: save_to_file(result)
4. 分析结果: analyzer.analyze(results)
5. 生成报告: reporter.generate(report_data)
```

### 3.2 requester.py - HTTP通信中枢

**设计目标**:
- 统一HTTP请求接口
- Session管理(复用TCP连接)
- URL净化(剥离fragment)
- Cookie处理

**核心类**:
```python
class Requester:
    """HTTP请求封装类"""

    def send(self, method, url, params=None, data=None, headers=None)
    def clean_url(self, url, keep_fragment=False)
    def set_cookie(self, cookie: str)
    def close(self)
```

**技术实现**:
- 基于`requests.Session`
- 自动处理重定向
- 超时控制(10s)
- 异常捕获与重试

### 3.3 baseline.py - 基准测试组件

**设计目标**:
- 建立正常响应的指纹
- 对比异常响应
- 计算偏差(长度/时间/状态码)

**核心类**:
```python
class Baseline:
    """基准测试组件"""

    def establish(self, target, test_value, requests=5) -> Dict
    def compare(self, baseline, response, threshold=0.3) -> Dict
    def fingerprint(self, response) -> str
```

**基准建立流程**:
```
1. 发送5次正常请求(test_value='FUZZ_TEST_BASELINE')
2. 计算平均值:
   - status: 第一个响应的状态码
   - length: 响应长度平均值
   - time: 响应时间平均值
   - content: 第一个响应的内容
3. 返回baseline字典
```

**异常判定规则**:
- 状态码异常: response.status != baseline.status
- 长度异常: |response.len - baseline.len| / baseline.len > 30%
- 时间异常: response.time > 5.0s
- 综合异常: 任一条件满足即判定为异常

### 3.4 sqli_engine.py - SQL注入引擎

**设计目标**:
- 实现三种检测方式: 报错、布尔盲注、时间盲注
- 基于Baseline对比结果
- 严重性评级(High/Medium)

**核心类**:
```python
class SQLiEngine(BaseEngine):
    """SQL注入检测引擎"""

    def detect(self, target, payloads) -> List[Dict]
    def _detect_error(self, target, payloads, param) -> List[Dict]
    def _detect_boolean(self, target, param) -> List[Dict]
    def _detect_time(self, target, param) -> List[Dict]
```

**检测逻辑**:

**1. 报错注入**:
```
流程:
1. 遍历前100个payloads
2. 注入到参数中
3. 检查响应是否包含SQL错误关键词:
   - "SQL syntax"
   - "mysql_fetch"
   - "ORA-01756"
4. 发现关键词 → 记录漏洞(Severity: High)
```

**2. 布尔盲注**:
```
流程:
1. 使用预定义payload对:
   - ("AND 1=1", "AND 1=2")
   - ("' AND '1'='1", "' AND '1'='2")
2. 对比True和False的响应长度
3. 长度差异 > 100字符 → 记录漏洞(Severity: Medium)
```

**3. 时间盲注**:
```
流程:
1. 使用时间payload:
   - "AND SLEEP(5)"
   - "WAITFOR DELAY '00:00:05'"
2. 检查响应时间
3. 时间 > 4秒 → 记录漏洞(Severity: Medium)
```

### 3.5 xss_engine.py - XSS检测引擎

**设计目标**:
- 检测反射型XSS
- 分析payload上下文环境
- 上下文感知(html_tag/html_attr/javascript/event_handler)

**核心类**:
```python
class XSSEngine(BaseEngine):
    """XSS检测引擎"""

    def detect(self, target, payloads) -> List[Dict]
    def _detect_reflected(self, target, payloads, param) -> List[Dict]
    def _is_payload_reflected(self, response_text, payload) -> bool
    def _analyze_context(self, response_text, payload) -> str
```

**检测逻辑**:
```
流程:
1. 遍历前200个payloads
2. 注入到参数中
3. 检查payload是否被反射到响应中
4. 如果反射:
   4.1 提取上下文(前后50字符)
   4.2 判断上下文类型:
       - script_tag: <script>...</script>
       - html_tag: <tag>...</tag>
       - event_handler: onXXX="..."
       - text_content: 纯文本
   4.3 记录漏洞(Severity: Medium)
```

### 3.6 payload_manager.py - 载荷管理器

**设计目标**:
- 统一载荷加载接口
- 支持多种输入格式
- 按攻击类型分组

**核心类**:
```python
class PayloadManager:
    """载荷管理器"""

    def load(self, mode='common', source=None) -> Dict[str, List[str]]
    def _load_cvae(self) -> Dict[str, List[str]]
    def _load_common(self) -> Dict[str, List[str]]
    def _load_custom(self, source) -> Dict[str, List[str]]
```

**载荷路径配置**:
```python
PAYLOAD_PATHS = {
    'cvae': 'Data/processed/fuzzing/refined_payloads.txt',
    'common': 'payload/dict/',
    'seclists': 'Data/payload/FuzzLists/'
}
```

**加载规则**:
```
1. CVAE模式:
   - 加载精锐载荷文件
   - 返回 {'ALL': [payload1, payload2, ...]}

2. 普通模式:
   - 遍历payload/dict/目录
   - 按文件名分组:
     - sqli.txt → {'SQLi': [...]}
     - xss.txt → {'XSS': [...]}
     - cmdi.txt → {'CMDi': [...]}

3. 自定义模式:
   - 支持单文件: ./payload/sqli.txt
   - 支持文件夹: ./payload/
   - 支持通配符: ./payload/*.txt
   - 支持逗号分隔: sqli.txt,xss.txt
```

### 3.7 analyzer.py - 结果分析器

**设计目标**:
- 去重(避免重复漏洞)
- 严重性评级(High > Medium > Low)
- 统计信息生成

**核心类**:
```python
class Analyzer:
    """结果分析器"""

    def analyze(self, results: List[Dict]) -> Dict
    def _deduplicate(self, results) -> List[Dict]
    def _rank(self, results) -> List[Dict]
    def _statistics(self, results) -> Dict
```

**去重逻辑**:
```
生成唯一标识: f"{vuln_type}_{param}_{payload[:50]}"
例如: SQLi_id_' OR 1=1--
```

**评级规则**:
```
High:    报错注入、命令执行
Medium:  布尔盲注、时间盲注、XSS
Low:     信息泄露
```

---

## 4. 技术决策与约束

### 4.1 引擎调度策略

**当前实现(第一版)**:
- ✅ **串行调度**: 先SQLi，后XSS
- ✅ **单目标多参数并发**: 一个目标的多个参数并发测试

**后续扩展(第二版)**:
- 🔄 **并行调度**: SQLi和XSS同时运行
- 🔄 **多目标并发**: 多个目标同时测试

**实现方式**:
```python
# 第一版(串行)
for engine_name in ['sqli', 'xss']:
    engine = self.engines[engine_name]
    results = engine.detect(target, payloads)

# 第二版(并行)
from concurrent.futures import ThreadPoolExecutor
with ThreadPoolExecutor(max_workers=2) as executor:
    futures = {
        executor.submit(engine.detect, target, payloads): name
        for name, engine in self.engines.items()
    }
```

### 4.2 并发策略

**当前实现(第一版)**:
- ✅ **单目标多参数并发**:
  - 使用`ThreadPoolExecutor`
  - 最大线程数: 20(可配置)
  - 适用场景: 测试单个URL的多个参数

**后续扩展(第二版)**:
- 🔄 **多目标并发**:
  - 多个URL同时测试
  - 需要考虑: 基准复用、结果聚合

**实现示例**:
```python
# 单目标多参数并发
from concurrent.futures import ThreadPoolExecutor, as_completed

def test_parameter(param_name):
    # 注入payload
    result = inject(param_name, payload)
    return result

with ThreadPoolExecutor(max_workers=20) as executor:
    futures = [executor.submit(test_parameter, p) for p in params]
    for future in as_completed(futures):
        result = future.result()
        # 实时写入文件
        save_result(result)
```

### 4.3 载荷分配策略

**CVAE模式**:
- ✅ 所有引擎共享同一份载荷
- ✅ 载荷来源: `Data/processed/fuzzing/refined_payloads.txt`
- ✅ 载荷格式: `{'ALL': [payload1, payload2, ...]}`

**普通模式**:
- ✅ 按攻击类型加载
- ✅ SQLi引擎使用`payload/dict/sqli.txt`
- ✅ XSS引擎使用`payload/dict/xss.txt`

**示例**:
```python
# CVAE模式
payloads = payload_mgr.load(mode='cvae')
# {'ALL': ['" OR 1=1--', "' AND '1'='1", ...]}

# SQLi引擎使用前100个
sqli_payloads = payloads['ALL'][:100]

# XSS引擎使用前200个
xss_payloads = payloads['ALL'][:200]
```

### 4.4 结果存储策略

**实时写入**:
- ✅ 每发现一个漏洞立即写入JSON文件
- ✅ 防止程序中断导致数据丢失
- ✅ 文件路径: `Data/scan_results/{site_name}/scan_results.json`

**写入格式**:
```json
{
  "scan_info": {
    "timestamp": "2025-12-25 15:30:00",
    "target": "http://target.com",
    "engine": "basic",
    "mode": "cvae"
  },
  "vulnerabilities": [
    {
      "vuln_type": "SQLi",
      "method": "Error-Based",
      "severity": "High",
      "payload": "' OR 1=1--",
      "param": "id",
      "evidence": "SQL syntax error",
      "response": {...}
    }
  ],
  "statistics": {
    "total_vulns": 10,
    "high_severity": 3,
    "medium_severity": 5,
    "low_severity": 2
  }
}
```

### 4.5 进度显示方案

**技术选型**: `tqdm`

**显示内容**:
```
[SQLi] Target 1/5 | ████████░░░░░░░░ 40% | 40/100 payloads | Vulns: 2
[XSS]  Target 1/5 | ████░░░░░░░░░░░░ 20% | 40/200 payloads | Vulns: 1
```

**多进度条实现**:
```python
from tqdm import tqdm

# 单引擎进度条
pbar = tqdm(total=100, desc=f"[SQLi] Target {i+1}/{n}")

# 更新进度
pbar.update(1)
pbar.set_postfix({'Vulns': vuln_count})
```

### 4.6 MVP范围约束

**包含功能**:
- ✅ BaseFuzz完整实现
- ✅ SQLi + XSS双引擎
- ✅ Spider集成
- ✅ CVAE模式
- ✅ 串行调度
- ✅ 单目标多参数并发
- ✅ 实时进度显示
- ✅ 实时结果写入

**不包含功能**:
- ❌ Boofuzz引擎
- ❌ ffuf引擎
- ❌ 多引擎并行
- ❌ 多目标并发
- ❌ 命令注入引擎(CMDi)
- ❌ HTML报告生成

---

## 5. 数据流设计

### 5.1 完整数据流

```
用户执行命令
    ↓
fuzzmain.py 解析参数
    ↓
选择引擎: engine='basic'
    ↓
调用 Spider.crawl() 或加载缓存
    ↓
生成 FuzzTarget 列表
    ↓
初始化 Engine
    ├─→ Requester (HTTP通信)
    ├─→ Baseline (基准测试)
    ├─→ PayloadManager (载荷管理)
    └─→ Analyzer (结果分析)
    ↓
加载漏洞引擎: load(['sqli', 'xss'])
    ├─→ SQLiEngine
    └─→ XSSEngine
    ↓
加载载荷: PayloadManager.load(mode='cvae')
    ↓
开始扫描
    ↓
for target in targets:
    ├─→ 建立基准: baseline.establish(target)
    │
    ├─→ 串行执行引擎:
    │   ├─→ SQLiEngine.detect(target, payloads)
    │   │   ├─→ _detect_error()
    │   │   ├─→ _detect_boolean()
    │   │   └─→ _detect_time()
    │   │
    │   └─→ XSSEngine.detect(target, payloads)
    │       └─→ _detect_reflected()
    │
    └─→ 实时写入: save_result(result)
    ↓
扫描结束
    ↓
分析结果: analyzer.analyze(results)
    ├─→ 去重
    ├─→ 评级
    └─→ 统计
    ↓
生成报告: reporter.generate(report_data)
    ├─→ JSON报告
    └─→ 终端输出
    ↓
完成
```

### 5.2 单个漏洞检测流程

以SQL注入检测为例:

```
目标: http://target.com/?id=1&name=test
载荷: ["' OR 1=1--", "' AND '1'='1", ...]

流程:
1. 建立基准
   - GET请求: ?id=FUZZ_TEST_BASELINE&name=FUZZ_TEST_BASELINE
   - 重复5次取平均
   - baseline = {status: 200, length: 1234, time: 0.5s}

2. 遍历参数
   for param in ['id', 'name']:

       3. 遍历载荷
       for payload in payloads[:100]:

           4. 注入载荷
           - GET请求: ?id=<payload>&name=test

           5. 获取响应
           response = requester.send('GET', url, params)

           6. 报错检测
           if "SQL syntax" in response.text:
               发现漏洞 → 记录结果

           7. 布尔检测
           true_resp = inject("AND 1=1")
           false_resp = inject("AND 1=2")
           if |true_len - false_len| > 100:
               发现漏洞 → 记录结果

           8. 时间检测
           if response.time > 4.0s:
               发现漏洞 → 记录结果

           9. 实时写入
           if 发现漏洞:
               save_result(result, 'scan_results.json')

           10. 更新进度
           pbar.update(1)
```

---

## 6. 扩展性设计

### 6.1 新增漏洞引擎

**步骤**:
1. 在`BaseFuzz/engines/`下创建新文件
2. 继承`BaseEngine`抽象基类
3. 实现`detect()`方法
4. 在`engine.py`中注册

**示例**:
```python
# BaseFuzz/engines/cmdi_engine.py
from .base_engine import BaseEngine

class CMDiEngine(BaseEngine):
    """命令注入检测引擎"""

    def detect(self, target, payloads):
        results = []

        # Unix命令注入
        unix_results = self._detect_unix(target, payloads)
        results.extend(unix_results)

        # Windows命令注入
        windows_results = self._detect_windows(target, payloads)
        results.extend(windows_results)

        return results

    def _detect_unix(self, target, payloads):
        # 实现
        pass

    def _detect_windows(self, target, payloads):
        # 实现
        pass
```

### 6.2 新增载荷源

**步骤**:
1. 在`payload_manager.py`中添加路径配置
2. 实现加载逻辑

**示例**:
```python
PAYLOAD_PATHS = {
    'cvae': 'Data/processed/fuzzing/refined_payloads.txt',
    'common': 'payload/dict/',
    'seclists': 'Data/payload/FuzzLists/',
    'custom': '/path/to/custom/payloads/'  # 新增
}

def _load_custom(self, source):
    # 实现自定义加载逻辑
    pass
```

### 6.3 新增结果分析维度

**步骤**:
1. 在`analyzer.py`中添加新方法
2. 扩展结果数据结构

**示例**:
```python
class Analyzer:
    def analyze(self, results):
        # 原有分析
        unique_results = self._deduplicate(results)
        ranked_results = self._rank(unique_results)

        # 新增: 置信度评分
        scored_results = self._score_confidence(ranked_results)

        return {
            'vulnerabilities': scored_results,
            'statistics': self._statistics(scored_results)
        }

    def _score_confidence(self, results):
        # 计算每个漏洞的置信度(0.0-1.0)
        pass
```

---

## 7. 实施路线图

### 7.1 第一阶段(Week 1-2): 基础框架

**目标**: 搭建BaseFuzz核心框架

**任务**:
- [ ] 实现engine.py(调度器)
- [ ] 实现requester.py(HTTP通信)
- [ ] 实现baseline.py(基准测试)
- [ ] 实现payload_manager.py(载荷管理)
- [ ] 实现utils/logger.py(日志系统)
- [ ] 实现utils/progress.py(进度显示)
- [ ] 编写单元测试

**交付物**:
- ✅ BaseFuzz核心框架代码
- ✅ 单元测试覆盖率 > 80%
- ✅ 技术文档

### 7.2 第二阶段(Week 3-4): 检测引擎

**目标**: 实现SQLi和XSS检测引擎

**任务**:
- [ ] 实现base_engine.py(抽象基类)
- [ ] 实现sqli_engine.py(报错/布尔/时间)
- [ ] 实现xss_engine.py(反射型/上下文)
- [ ] 集成到engine.py
- [ ] 编写单元测试

**交付物**:
- ✅ SQLi检测引擎
- ✅ XSS检测引擎
- ✅ 集成测试报告

### 7.3 第三阶段(Week 5-6): Spider集成

**目标**: 集成爬虫模块

**任务**:
- [ ] 完善Fuzz/spider.py
- [ ] 实现缓存机制
- [ ] 实现多目标支持
- [ ] 集成到fuzzmain.py
- [ ] 端到端测试

**交付物**:
- ✅ Spider集成完成
- ✅ 端到端测试通过
- ✅ 用户文档

### 7.4 第四阶段(Week 7-8): CVAE模式

**目标**: 实现CVAE载荷支持

**任务**:
- [ ] 扩展payload_manager.py支持CVAE
- [ ] 实现载荷共享机制
- [ ] 性能优化(大量载荷)
- [ ] 对比测试(CVAE vs 普通模式)

**交付物**:
- ✅ CVAE模式实现
- ✅ 性能对比报告
- ✅ 使用文档

### 7.5 第五阶段(Week 9-10): 结果分析

**目标**: 实现结果分析与报告

**任务**:
- [ ] 实现analyzer.py(去重/评级/统计)
- [ ] 实现reporter.py(JSON报告)
- [ ] 实现实时写入机制
- [ ] 性能优化

**交付物**:
- ✅ 结果分析模块
- ✅ JSON报告生成
- ✅ 完整的用户手册

### 7.6 后续版本(第二版)

**目标**: 集成Boofuzz和ffuf

**任务**:
- [ ] 实现Fuzz/BooFuzz/模块
- [ ] 实现Fuzz/ffuf/模块
- [ ] 实现多引擎并行调度
- [ ] 实现多目标并发
- [ ] 实现HTML报告

**交付物**:
- ✅ 三引擎并行系统
- ✅ HTML报告
- ✅ 高级用户文档

---

## 8. 关键技术栈

### 8.1 核心依赖

```python
# requirements.txt
# HTTP通信
requests>=2.31.0

# HTML解析
beautifulsoup4>=4.12.0

# 进度显示
tqdm>=4.66.0

# 彩色输出
colorama>=0.4.6

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# 模糊测试
boofuzz>=0.2.0  # 第二版

# YAML配置
pyyaml>=6.0

# 类型检查
typing-extensions>=4.7.0
```

### 8.2 Python版本

- **最低版本**: Python 3.8
- **推荐版本**: Python 3.10+
- **开发版本**: Python 3.11

### 8.3 系统要求

- **操作系统**: Windows/Linux/macOS
- **内存**: 最低4GB，推荐8GB
- **磁盘**: 最低2GB可用空间
- **网络**: 目标网络可达

---

## 9. 测试策略

### 9.1 单元测试

**覆盖范围**:
- ✅ 核心模块(engine/requester/baseline)
- ✅ 检测引擎(sqli/xss)
- ✅ 工具函数(utils)

**测试框架**: `pytest`

**覆盖率目标**: > 80%

### 9.2 集成测试

**测试场景**:
- ✅ 单目标扫描
- ✅ 多目标扫描
- ✅ CVAE模式
- ✅ 普通模式
- ✅ 实时写入

**测试环境**:
- DVWA (Damn Vulnerable Web App)
- Pikachu (本地靶场)

### 9.3 性能测试

**测试指标**:
- ✅ 扫描速度 (req/s)
- ✅ 内存占用
- ✅ CPU占用
- ✅ 并发性能

**测试工具**:
- `memory_profiler`
- `cProfile`

---

## 10. 风险与挑战

### 10.1 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| HTTP请求超时 | 高 | 实现重试机制、超时控制 |
| 目标WAF拦截 | 中 | 添加延迟、降低并发 |
| 内存溢出 | 中 | 限制载荷数量、分批处理 |
| 误报率高 | 低 | 二次验证、置信度评分 |

### 10.2 工程风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 开发周期长 | 中 | MVP优先、迭代开发 |
| 代码复杂度高 | 中 | 模块化设计、代码审查 |
| 测试覆盖不足 | 低 | 单元测试、集成测试 |

---

## 11. 附录

### 11.1 术语表

| 术语 | 解释 |
|------|------|
| FuzzTarget | 模糊测试目标数据结构(URL+方法+参数) |
| Baseline | 正常响应的基准指纹 |
| Payload | 测试载荷(攻击代码) |
| CVAE模式 | 使用AI生成的精锐载荷 |
| 普通模式 | 使用专家字典载荷 |
| 串行调度 | 引擎按顺序执行(先SQLi后XSS) |
| 并发测试 | 多个参数/目标同时测试 |

### 11.2 参考文档

- [第一~三阶段技术白皮书](./prompt指导.md)
- [Web模糊测试框架构想](./Web模糊测试框架构想探讨.md)
- [ffuz官方文档](https://github.com/ffuf/ffuf)
- [Boofuzz官方文档](https://github.com/tparise/boofuzz)

### 11.3 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2025-12-25 | 初始版本，第四阶段架构设计 |

---

## 12. 联系方式

- **作者**: 老王 (暴躁技术流)
- **项目**: CVDBFuzz
- **文档版本**: v1.0
- **最后更新**: 2025-12-25

---

**文档结束**
